'Write a program to print area of rectangle three times by taking user input?'

length = float(input('enter the value of length'))
breadth = float(input('enter the value of breadth'))
ares = length*breadth
for i in range(3):
